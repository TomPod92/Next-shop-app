import CategoryList from "./_components/CategoriesList/CategoryList";

export const metadata = {
  title: "Category list page",
};

const MainPage = () => {
  return <CategoryList />;
};

export default MainPage;
